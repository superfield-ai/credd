# CI Policy

## Documentation Changes

Documentation changes (e.g., Markdown files in `docs/`, `README.md`) do not require CI runs and can be merged without executable tests. 

This policy ensures that documentation updates remain frictionless and are not blocked by CI pipelines that execute tests against source code.
